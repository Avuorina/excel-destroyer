# 『ExcelDestroyer』開発完了・引き継ぎ報告書（HANDOVER.md）

本ドキュメントは、本作のこれまでの開発における膨大な進化、解決された致命的な演算バグ、および極限まで磨き上げられたゲームデザイン（難易度バランス調整）の歴史をまとめた公式引き継ぎ報告書です。

---

## 1. 🌟 完了した偉業 ＆ 実装機能サマリー

本フェーズにおいて、ゲームの純度、テンポ、安定性、そしてインフレカタルシスを最高峰まで高める以下の大改修が行われました。

* **２Ｄグリッドの超大拡張（5列6行設計）**:
  * グリッドを **横5列（A〜E列）**、**縦6行（1〜6行）** の宇宙規模へと完全に大解放！
  * A（定数） $\rightarrow$ B（SUM） $\rightarrow$ C（PRODUCT） $\rightarrow$ D（FACT/POWER） $\rightarrow$ E（TOWER）の「5段インフレ計算要塞」が開通しました。
* **セル左ダブルクリック「詳細インスペクター」システム**:
  * マウス位置（画面外はみ出し防止付き）にガラスモルフィズム調のSFインスペクターポップアップを生成。
  * 巨大数比率計算（％）による、各セルの「全体秒間売上（DPS）への貢献度」のリアルタイム算出。
  * A列ダブルクリック時に、ショップを往復せずその場で直接定数をレベルアップできる「セルダイレクト定数ハック強化ボタン」を完全接続。
* **クリーンアップショップUI（引き算の美学）**:
  * SUM、PRODUCT、FACTアンロックなど、1回限り（`max == 1`）のアップグレードを購入完了した瞬間に、ショップからスッキリと消去する極上UXを実装。
  * 行を増やすごとに、ショップに対応する定数強化カード（A4〜A6 値+0.1）が動的ににゅっと出現する出現チェーンを構築。

---

## 2. ⚡ 撃破した致命的バグ ＆ 解決アプローチ

本作が天文学的インフレゲームだからこそ発生した、非常に技術的・数学的に美しい2大バグを完璧に射貫き、一撃で消滅させました。

### 🐞 バグ1：FACT（階乗）巨大数受け取り時の 1.0 融解（バグ化）不具合
* **現象**: `C1` セルが `2.31e71` のように巨大な場合、それを参照する `=FACT(C1)` のセル（C2、C3）が、計算結果 **`1.0`** に化けてしまっていた。
* **原因**: `int(c.display_value.to_float())` を実行した際、表現限界を超えた float が `INF` となり、それを `int` キャストした瞬間にオーバーフローして `0` や不整合な負数に崩壊。`clamp(n, 1, 100)` によってループ回数 `n` が強制的に最低の `1` に制限され、`1.0` を1回掛け算しただけの値（1.0）が返されていた。
* **解決法**: 引数の指数 `exponent >= 1`（10以上）の超巨大数の場合、実数ループを安全にバイパスし、**階乗の計算項 `n` を安全限界の最大値 `100`（FACT(100) = 9.33e157）に自動固定**する安全バイパスロジックを [FormulaEngine.gd](file:///d:/PUTI%20GAME/ExcelDestroyer/excel-destroyer/scripts/FormulaEngine.gd) に実装。インフレが `1.0` に融解することなく、宇宙規模でバチバチに連鎖するようになりました！

### 🐞 バグ2：DPS（Coins Per Second）が A2 以降の定数セルを完全に無視していた不具合
* **現象**: 行を追加し、`A2` に値を設定して `=SUM(A1, A2)` の結果が正しく増えても、画面右上の「DPS:」が `A2` の値を完全に無視して低く計算されていた。
* **原因**: 生産効率（`current_max`）を計算する際、基礎売上として加算する処理が `cells.has("A1")` だけのハードコーディングになっており、`A2`〜`A6` の値が完全に売上計算から締め出されていた。
* **解決法**: `for r in range(1, max_rows + 1):` による動的ループ走査を新設！現在解放されているすべてのA列定数（A1〜A6）をリアルタイムに自動スキャンして合算する完璧な同期システムを [GameManager.gd](file:///d:/PUTI%20GAME/ExcelDestroyer/excel-destroyer/scripts/GameManager.gd) に実装。全ての定数値が余さずDPSに反映されるようになりました！

---

## 3. ⚖️ 磨き抜かれた「神ゲームバランス」調整

インフレの高揚感と、ハッキングパズルのやりごたえを極限まで調和させるため、緻密なバランス調整を施しました。

* **定数レベルアップを「値+1.0」から「値+0.1」へ引き締め**:
  * レベルアップごとの値上昇幅を **`+0.1`** にマイルド化。いかにも事務処理らしい細かな積み上げが、乗算（PRODUCT）や階乗（FACT）によって宇宙大爆発に相転移する抜群のカタルシスを演出。
  * ショップ（右メニュー）の表示ラベルも自動で **「A1 値+0.1」〜「A6 値+0.1」** へと完璧に同期。
* **計算速度x2 の個別急勾配化（コスト scale 6.0倍）**:
  * 生産DPSが文字通り「乗算的に2倍」になる最強アップグレード「計算速度x2」のコスト上昇倍率を、これまでの定数（3.5倍）よりさらに強烈な **`6.0倍`** に個別急勾配化！
  * 初回コストは `100.0` での買いやすさをキープしつつ、2回目以降は中盤のシートハックをやり込まなければ到達できない「完璧な中長期目標」に昇華。
* **初期オンボーディング（最初の行追加コスト）の快適化**:
  * 最初期の行追加（2行目の解放）コストを、これまでの `50.0` から **`40.0`** コインへ引き下げ。開始直後、定数を強化するか、すぐに2行目（A2）を拡張して「SUMアンロック」を解き放つかの選択肢が軽快なテンポで提示されるよう最適化。
* **D列・E列の大拡張コスト設定**:
  * 動的解放リミットを5列目まで開通させた上で、コストを D列：`5,000.0`、E列：`100,000.0` とスリリングに引き上げ、中〜終盤への壮大な目標ラインを提示。
